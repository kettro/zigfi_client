/*
 * Accounts Controller
 * Routes for managing and accessing accounts
 * features:
 * > login
 * > storing mqtt servers
 * > storing passwords
 */
var express = require('express');
var router = express.Router();

router.get('/', (req, res, next) => {

});

module.exports = router;
